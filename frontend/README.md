
Use bootstrap for styling, use bootstrap Modal (modal.dialog)
keep consistent style in overall project ( like colors, button style

Client dashboard not functional
Appointments approved without checking schedules conflict

My client creation logic changes. when new client creates the data if client saved in users table, where all users data
(admin, business_owner, operational_staff, service_staff) already stored. so now for client queries and client side operations
perform with userCode and checking user_type is client. I fix my backend code to a implement this logic, but you check if something is missing
fix it, then fix frontend according to this logic

in frontend also implement refreshToken & accessToken , logout functions for authentication. in my backend these are implemented